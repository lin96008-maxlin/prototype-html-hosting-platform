services:
  postgres:
    # GitHub Actions 使用 Docker Hub 官方缓存，避免 Public ECR 匿名拉取限流。
    # 该覆盖仅用于隔离测试，不影响生产环境的数据库镜像。
    image: mirror.gcr.io/library/postgres:16-alpine
    ports:
      - "127.0.0.1:${POSTGRES_HOST_PORT:-55432}:5432"
  app:
    environment:
      STORAGE_RESERVE_RATIO: 0.01
